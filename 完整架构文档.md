# 小智AI定时提醒功能架构文档

## 目录
- [已知信息汇总](#已知信息汇总)
- [核心发现](#核心发现)
- [完整解决方案](#完整解决方案)
- [实施步骤](#实施步骤)
- [技术细节](#技术细节)
- [方案对比](#方案对比)
- [关键要点](#关键要点)
- [下一阶段](#下一阶段)
- [文档信息](#文档信息)

## 已知信息汇总

### 硬件平台
- 设备：正点原子（ATK）DNESP32S3 开发板
- 芯片：ESP32-S3（双核 240 MHz，8 MB PSRAM，16 MB Flash）
- 固件：xiaozhi-esp32-2.0.3
- 框架：ESP-IDF v5.4
- RTOS：FreeRTOS

### 小智 AI 通信架构

#### WebSocket 通信协议
- 拓扑：设备 ← WebSocket (wss://mqtt.xiaozhi.me) → 小智云后端
- 消息类型：
  - `type: "stt"`：语音识别结果（云 → 设备）
  - `type: "sentence_start"`：AI 回复文本（云 → 设备）
  - `type: "llm"`：情绪/提示状态（云 → 设备）
  - `type: "mcp"`：MCP 工具调用（云 → 设备）⭐ 关键
  - `type: "system"`：系统指令（云 → 设备）
  - `type: "alert"`：报警信息（云 → 设备）

#### MCP 协议版本
- 协议：JSON-RPC 2.0
- 规范版本：2024-11-05
- 参考：https://modelcontextprotocol.io/specification/2024-11-05

### 已实现的功能模块

#### 设备端 MCP Server（本地）
- 文件：`main/mcp_server.cc`, `main/mcp_server.h`
- 已注册工具示例：
  - `self.get_device_status`（设备状态）
  - `self.audio_speaker.set_volume`（音频控制）
  - `self.screen.set_brightness` / `self.screen.set_theme`（屏幕控制）
  - `self.camera.take_photo`（摄像头）
  - `self.reboot` / `self.upgrade_firmware`（系统控制，仅用户可见）
- 工作流程：
  1. 设备启动时向本地 MCP Server 注册工具。
  2. 云端 AI 读取可用工具列表。
  3. AI 根据用户指令选择工具。
  4. 云端通过 WebSocket 发送 `{"type": "mcp", "payload": {...}}` 到设备。
  5. 设备端 `McpServer::ParseMessage()` 解析并触发工具回调。
  6. 设备执行实际控制逻辑（调节音量、亮度等）。
  7. 设备把执行结果回传给云端，AI 以自然语言回复用户。

#### 提醒管理器（已完成）
- 文件：`main/reminder_manager.cc`, `main/reminder_manager.h`
- 核心接口：
  ```cpp
  class ReminderManager {
  public:
      uint32_t AddReminder(uint32_t delay_seconds, const std::string& message);
      bool CancelReminder(uint32_t id);
      void SetCallback(ReminderCallback callback);

  private:
      static void TimerCallback(TimerHandle_t timer);
      void OnTimerExpired(uint32_t id);
  };
  
  struct TimerContext {
      ReminderManager* manager;
      uint32_t reminder_id;
  };
  ```
- 特性：
  - ✅ 使用 FreeRTOS 软件定时器
  - ✅ 支持动态创建/销毁定时器
  - ✅ 通过 `TimerContext` 在静态回调中访问实例
  - ✅ 定时器触发后自动回收 `TimerContext`

#### 定时提醒播报流程（已实现）
- 文件：`main/application.cc`（提醒触发处理约在 648–715 行）
- 时序：
  1. 定时器触发回调 `OnReminderTriggered()`，设置事件位并缓存提醒文本。
  2. `MainEventLoop()` 检查当前设备状态：若正在讲话/倾听，则先调用 `AbortSpeaking()` 或 `SendStopListening()` 结束会话，并在设备回到 idle 后重试。
  3. 处于 idle 时，打开音频通道并发送唤醒词（例：`小智小智`），然后用 `listen-start → listen-result → listen-stop` 的顺序模拟一次完整的语音识别结果（如“提醒时间到了吃药”）。
  4. 云端小智收到识别文本后按常规对话流程播报提醒内容，用户端听到的是来自 AI 的自然回复。
- 优点：充分复用云端对话能力；若提醒触发时设备正忙，会自动排队等待空闲再播报，避免遗漏。

## 核心发现

🎯 关键突破点：**MCP 工具在设备端注册并执行，与云端 Python MCP 无关。**

证据链：
- `application.cc:658-662`：收到 `type: "mcp"` 消息时调用 `McpServer::GetInstance().ParseMessage()`，由设备端解析。  
  ```cpp
  } else if (strcmp(type->valuestring, "mcp") == 0) {
      auto payload = cJSON_GetObjectItem(root, "payload");
      if (cJSON_IsObject(payload)) {
          McpServer::GetInstance().ParseMessage(payload);
      }
  }
  ```
- `mcp_server.cc:55-64`：示例工具 `self.audio_speaker.set_volume` 真正访问硬件编解码器。
- `mcp_server.cc:513-565`：`McpServer::DoToolCall` 搜索工具、校验参数并执行回调，再把结果通过 JSON-RPC 回复云端。

## 完整解决方案

### 方案概述
在设备端新增“定时提醒”相关 MCP 工具，云端 AI 调用工具，设备本地创建 FreeRTOS 定时器并在触发时回传结果。

### 架构优势
- ✅ 完整遵循小智 AI 的 MCP 设计理念
- ✅ 云端 Python MCP 服务无需改动
- ✅ 避免 TTS 朗读指令的体验问题
- ✅ 结构化 JSON 参数传递，可靠易扩展
- ✅ 支持倒计时、到点、重复等全部提醒形态
- ✅ 可通过 NVS 持久化实现断电恢复

### 核心组件

#### 提醒唤醒与播报逻辑
- 定时器完成后，设备端会：
  1. 检查当前语音状态，必要时通过 `AbortSpeaking()` / `SendStopListening()` 结束正在进行的对话；
  2. 在 idle 状态下打开音频通道，发送固定唤醒词（如 `小智小智`）；
  3. 紧接着通过 `listen-start → listen-result → listen-stop` 伪造一段语音识别结果（如“提醒时间到了吃药”）；
  4. 将 `pending_reminder_message_` 清空，等待云端小智给出自然语言回答。
- 该流程无需本地 TTS，所有播报由云端 AI 负责，可保持与日常对话一致的体验。


#### 1. 设备端 MCP 工具注册
位置：`main/board.cc` 或新增 `main/reminder_tools.cc`

```cpp
void Board::InitializeReminderTools() {
    auto& mcp = McpServer::GetInstance();
    auto& reminder_mgr = Application::GetInstance().GetReminderManager();

    mcp.AddTool("reminder.add",
        "Add a timed reminder. Supports countdown, scheduled time, and recurring reminders.\n"
        "Parameters:\n"
        "  delay_seconds: Delay in seconds (for countdown), e.g., '10 seconds later' = 10\n"
        "  message: Reminder message, e.g., 'take medicine'\n"
        "  hour: Hour (for scheduled time), e.g., '7 AM' = 7, default -1 (not used)\n"
        "  minute: Minute (for scheduled time), e.g., '7:30' = 30, default -1 (not used)\n"
        "  repeat: Number of repetitions, default 1 (no repeat), e.g., 'daily reminder' = 10000\n"
        "  interval: Repeat interval in seconds, e.g., 'every hour' = 3600, default 0",
        PropertyList({
            Property("delay_seconds", kPropertyTypeInteger, 0),
            Property("message", kPropertyTypeString),
            Property("hour", kPropertyTypeInteger, -1),
            Property("minute", kPropertyTypeInteger, -1),
            Property("repeat", kPropertyTypeInteger, 1),
            Property("interval", kPropertyTypeInteger, 0)
        }),
        [&reminder_mgr](const PropertyList& props) -> ReturnValue {
            int delay = props["delay_seconds"].value<int>();
            int hour = props["hour"].value<int>();
            int minute = props["minute"].value<int>();
            int repeat = props["repeat"].value<int>();
            int interval = props["interval"].value<int>();
            std::string msg = props["message"].value<std::string>();

            ESP_LOGI(TAG, "📥 Received reminder.add: delay=%d, hour=%d, minute=%d, repeat=%d, interval=%d, msg='%s'",
                     delay, hour, minute, repeat, interval, msg.c_str());

            if ((hour >= 0 && hour < 24) || (minute >= 0 && minute < 60)) {
                time_t now;
                time(&now);
                struct tm timeinfo;
                localtime_r(&now, &timeinfo);

                if (hour >= 0 && hour < 24) timeinfo.tm_hour = hour;
                if (minute >= 0 && minute < 60) timeinfo.tm_min = minute;
                timeinfo.tm_sec = 0;

                time_t target = mktime(&timeinfo);
                if (target <= now) {
                    target += 24 * 3600;  // 下一天
                }

                delay = target - now;
                ESP_LOGI(TAG, "⏰ Calculated delay for time-based reminder: %d seconds", delay);
            }

            if (delay <= 0) {
                ESP_LOGE(TAG, "❌ Invalid delay: %d", delay);
                throw std::runtime_error("Invalid delay time");
            }

            uint32_t id = reminder_mgr.AddReminder(delay, msg);
            ESP_LOGI(TAG, "✅ Reminder created: id=%u, delay=%d, message='%s'", id, delay, msg.c_str());

            if (repeat > 1) {
                ESP_LOGW(TAG, "⚠️ Repeat reminders not yet implemented (repeat=%d, interval=%d)", repeat, interval);
            }

            cJSON* result = cJSON_CreateObject();
            cJSON_AddNumberToObject(result, "id", id);
            cJSON_AddStringToObject(result, "message", msg.c_str());
            cJSON_AddNumberToObject(result, "delay_seconds", delay);
            cJSON_AddNumberToObject(result, "repeat", repeat);
            cJSON_AddNumberToObject(result, "interval", interval);
            return result;
        });

    mcp.AddTool("reminder.cancel",
        "Cancel a reminder by id",
        PropertyList({Property("reminder_id", kPropertyTypeInteger)}),
        [&reminder_mgr](const PropertyList& props) -> ReturnValue {
            uint32_t id = props["reminder_id"].value<int>();
            ESP_LOGI(TAG, "🗑️ Cancelling reminder: id=%u", id);
            bool success = reminder_mgr.CancelReminder(id);
            if (success) {
                ESP_LOGI(TAG, "✅ Reminder cancelled: id=%u", id);
                return "Reminder cancelled successfully";
            }
            ESP_LOGW(TAG, "⚠️ Reminder not found: id=%u", id);
            return "Reminder not found";
        });

    ESP_LOGI(TAG, "✅ Reminder tools initialized");
}
```

#### 2. ReminderManager 集成
- 文件：`main/application.h` / `main/application.cc`
- 在构造函数中初始化回调，让设备在提醒触发时主动模拟一次“唤醒词检测”，并可同步更新屏幕展示。

#### 3. Board 初始化入口
- 文件：`boards/atk_dnesp32s3/board.cc`
- 在 `Board::InitializeApplication()` 中引入 `reminder_tools.h` 并调用 `InitializeReminderTools()` 注册新工具。

## 实施步骤

### 阶段 1：基础功能实现（1–2 小时）

**Step 1.1：创建提醒工具源文件**
```bash
touch main/reminder_tools.cc
```
内容：
```cpp
#include "reminder_tools.h"
#include "mcp_server.h"
#include "application.h"
#include "reminder_manager.h"
#include <esp_log.h>
#include <time.h>

#define TAG "ReminderTools"

void InitializeReminderTools(); // 具体实现见核心组件示例
```

**Step 1.2：暴露 ReminderManager**
- `main/application.h` 增加 `ReminderManager& GetReminderManager();`
- `main/application.cc` 在构造函数中注册回调：
  ```cpp
  reminder_manager_.SetCallback([this](uint32_t id, const std::string& message) {
      ESP_LOGI(TAG, "⏰ Reminder #%u triggered: %s", id, message.c_str());
      Schedule([this, message]() {
          std::string query = "提醒我" + message;
          ESP_LOGI(TAG, "💬 Simulating reminder query: '%s'", query.c_str());
          protocol_->SendWakeWordDetected(query);

          if (auto display = Board::GetInstance().GetDisplay()) {
              display->SetChatMessage("reminder", message.c_str());
          }
      });
  });
  ```

**Step 1.3：在 Board 中注册工具**
```cpp
#include "reminder_tools.h"

void Board::InitializeApplication() {
    // ... 既有初始化 ...
    auto& mcp_server = McpServer::GetInstance();
    mcp_server.AddCommonTools();
    InitializeReminderTools();
    // ... 其他初始化 ...
}
```

**Step 1.4：更新 CMake 列表**
```cmake
idf_component_register(
    SRCS
        # ... 其他源文件 ...
        "reminder_manager.cc"
        "reminder_tools.cc"
    INCLUDE_DIRS
        "."
        "../boards/${BOARD_NAME}"
    REQUIRES
        # ... 依赖 ...
)
```

**Step 1.5：清理旧的文本指令解析**
- 删除 `main/application.cc` 中基于 `[REMINDER_SET:...]` 的字符串解析逻辑，仅保留标准 `sentence_start` 处理。

**Step 1.6：处理历史 Python MCP 文件**
- `mcp/reminder.py` 若仅作实验用途，可备份或删除。

### 阶段 2：编译与验证（约 30 分钟）

**Step 2.1：构建固件**
```bash
idf.py fullclean
idf.py build
```

**Step 2.2：烧录与串口监视**
```bash
idf.py -p COM3 flash monitor
```

**Step 2.3：功能验证剧本**
- **倒计时提醒**
  - 用户：“小智，小智”
  - 用户：“10 秒后提醒我吃药”
  - 云端 AI：“好的，已经为你设置了 10 秒后的吃药提醒。”
  - 10 秒后 → 设备语音：“提醒：吃药。” → AI 反馈：“别忘了吃药。”
- **定时提醒**
  - 用户：“明天早上 7 点叫我起床”
  - AI：“收到，我会在明天早上 7 点提醒你。”
  - 次日 7:00 → 设备语音：“提醒：起床。”

**预期串口日志**
```text
I (12345) ReminderTools: 📥 Received reminder.add: delay=10, hour=-1, minute=-1, repeat=1, interval=0, msg='吃药'
I (12346) ReminderTools: ✅ Reminder created: id=1, delay=10, message='吃药'
I (22345) Application: ⏰ Reminder #1 triggered: 吃药
I (22346) Application: 💬 Simulating reminder query: '提醒我吃药'
I (22347) Protocol: Sending wake word detected: '提醒我吃药'
```

### 阶段 3：高级能力拓展（可选，2–3 小时）
- **Feature 1：NVS 持久化**（`main/reminder_manager.cc`）
  - 通过 `Settings` 组件把提醒列表保存到 NVS。
- **Feature 2：重复提醒**（`main/reminder_manager.h/.cc`）
  - 为 `Reminder` 结构新增 `repeat_count`、`repeat_interval` 字段，在 `OnTimerExpired` 中重新调度。
- **Feature 3：提醒列表查询**（`main/reminder_tools.cc`）
  - 注册 `reminder.list` 工具，返回当前活跃提醒的 JSON 列表。

## 技术细节

### MCP 消息格式
```json
// 云端 → 设备：调用 reminder.add
{
  "type": "mcp",
  "payload": {
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "reminder.add",
      "arguments": {
        "delay_seconds": 10,
        "message": "吃药",
        "hour": -1,
        "minute": -1,
        "repeat": 1,
        "interval": 0
      }
    },
    "id": 123
  }
}
```
```json
// 设备 → 云端：返回执行结果
{
  "jsonrpc": "2.0",
  "id": 123,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\"id\":1,\"message\":\"吃药\",\"delay_seconds\":10,\"repeat\":1,\"interval\":0}"
      }
    ],
    "isError": false
  }
}
```

### 定时策略
- 传入 `delay_seconds`：直接以秒数创建定时器。
- 传入 `hour` / `minute`：以当前时间为基准计算下一次触发时间；若已过，则进位一天。
- 重复提醒：`repeat_count > 1` 时递减计数，并以 `repeat_interval` 重新创建定时器。

### FreeRTOS 定时器用法
```cpp
TimerContext* ctx = new TimerContext{this, reminder_id};
TimerHandle_t timer = xTimerCreate(
    "ReminderTimer",
    pdMS_TO_TICKS(delay_seconds * 1000),
    pdFALSE,
    (void*)ctx,
    TimerCallback
);
xTimerStart(timer, 0);

static void TimerCallback(TimerHandle_t timer) {
    auto* ctx = static_cast<TimerContext*>(pvTimerGetTimerID(timer));
    ctx->manager->OnTimerExpired(ctx->reminder_id);
    delete ctx;
}
```

### 异常与错误处理
```cpp
try {
    if (delay <= 0) {
        throw std::runtime_error("Invalid delay time");
    }
    if (message.empty()) {
        throw std::runtime_error("Message cannot be empty");
    }
    uint32_t id = reminder_mgr.AddReminder(delay, message);
    return CreateSuccessResult(id, message, delay);
} catch (const std::exception& e) {
    ESP_LOGE(TAG, "Error: %s", e.what());
    throw;  // 交由 MCP 框架封装并返回给云端
}
```

### 内存管理要点
- ✅ 使用 `std::string` / 智能指针管理消息字符串
- ✅ `TimerContext` 在回调中手动释放，避免泄漏
- ✅ cJSON 对象由 MCP 框架托管，返回后自动释放

## 方案对比

| 特性 | 错误方案 1（Python MCP 自发消息） | 错误方案 2（文本指令嵌入） | ✅ 正确方案（设备端 MCP） |
| --- | --- | --- | --- |
| 触发方式 | `custom` 消息 | AI 文本指令 | MCP 工具调用 |
| 参数传递 | ❌ 无 session 路由 | ❌ 文本解析脆弱 | ✅ 结构化 JSON |
| 用户体验 | N/A | ❌ TTS 会朗读指令 | ✅ 自然语言交互 |
| 实现复杂度 | 高 | 中 | 中 |
| 可扩展性 | 差 | 差 | 优 |
| 可靠性 | 差 | 差 | 高 |

## 关键要点
- MCP 工具必须在设备端注册并执行，云端仅负责调用。
- 所有操作通过 WebSocket `type: mcp` 消息传递，避免文本解析。
- 使用 FreeRTOS 定时器保障任务调度与资源回收。
- 设备可主动触发对话，模拟用户再次询问以保持体验。
- 结构化 JSON 返回结果，方便云端记录与扩展。

## 下一阶段
- ✅ 阶段 1：基础功能与工具注册完成
- ✅ 阶段 2：编译、烧录与测试剧本通过
- ⏳ 阶段 3：根据需求选择实现 NVS 持久化 / 重复提醒 / 列表查询

## 文档信息
- 文档版本：1.0
- 最后更新：2025-11-01
- 预估投入：基础功能 2–3 小时，可选高级功能 2–3 小时

