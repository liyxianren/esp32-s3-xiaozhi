# 小智AI定时提醒功能实施文档

## 文档概述
- 目标：按照 `完整架构文档.md` 的设计，在设备端实现可用的定时提醒能力，并完成交付验证。
- 范围：`main/` 组件（ReminderManager、McpServer、Board）、`boards/atk_dnesp32s3/`、`main/CMakeLists.txt`、构建与烧录流程。
- 交付标准：基础提醒功能上线、串口日志验证通过、旧有文本解析方案彻底移除。

## 实施里程碑
| 阶段 | 主要目标 | 关注重点 |
| --- | --- | --- |
| 阶段 0 | 准备工作 | 环境、分支、依赖确认 |
| 阶段 1 | 设备端提醒工具开发 | MCP 工具注册、ReminderManager 集成 |
| 阶段 2 | 构建与联调 | 编译、烧录、串口测试 |
| 阶段 3 | 可选增强 | NVS 持久化、重复提醒、列表查询 |

## TODO 列表

### 基础上线（必须完成）
- [x] 建立工作分支并同步最新代码
- [x] 新增 `reminder_tools.h/.cc` 并实现 `InitializeReminderTools`
- [x] 为 `reminder.add` / `reminder.cancel` 注册 MCP 工具
- [x] 处理 `hour`/`minute` → `delay_seconds` 的时间换算
- [x] 在异常情况下抛出错误并输出 ESP_LOG 日志
- [x] 将 `ReminderManager` 对接到 `Application`，补充触发回调
- [x] 更新 `Board::InitializeApplication()` 引入提醒工具
- [x] 修改 `main/CMakeLists.txt` 引入新的源文件
- [x] 移除 `[REMINDER_SET:...]` 文本解析遗留逻辑
- [x] 备份或清理 `mcp/reminder.py` 中旧实验代码
- [ ] 调整定时提醒播报：唤醒小智并同步识别文本由云端播报

### 联调与验证（必须完成）
- [ ] 运行 `idf.py fullclean && idf.py build`
- [ ] 烧录并使用 `idf.py -p <PORT> flash monitor`
- [ ] 覆盖倒计时提醒测试用例
- [ ] 覆盖定时提醒测试用例（跨天）
- [ ] 记录并归档串口日志

### 可选增强（按需）
- [ ] ReminderManager 写入/读取 NVS，实现断电恢复
- [ ] 支持重复提醒（repeat + interval）
- [ ] 提供 `reminder.list` MCP 工具
- [ ] 优化屏幕提示 UI（如显示提醒列表）

## 阶段 0：准备工作
1. **环境确认**：确保 ESP-IDF v5.4、Python 环境、串口驱动已就绪。
2. **代码基线**：从 `master`/`main` 同步最新提交，创建 `feature/reminder-mcp` 分支。
   - ✅ 2025-11-01: 已创建 `feature/reminder-mcp` 分支，当前工作基于该分支。
3. **依赖核对**：确认 `reminder_manager.*`、`mcp_server.*`、`application.*` 当前可编译，避免历史残留问题。
4. **备份旧方案**：将 `mcp/reminder.py` 复制为 `mcp/reminder.py.backup` 以备查阅。
   - ✅ 2025-11-01: 已通过 `git mv` 重命名为 `mcp/reminder.py.backup`。

## 阶段 1：设备端提醒工具开发
### Step 1.1 新增提醒工具文件
- 创建 `main/reminder_tools.h` / `main/reminder_tools.cc`。
- `InitializeReminderTools()` 内部完成：
  - 调用 `McpServer::GetInstance()`、`Application::GetInstance().GetReminderManager()`。
  - 注册 `reminder.add`，处理参数、时间换算、异常校验、返回 cJSON 结果。
  - 注册 `reminder.cancel`，调用 `ReminderManager::CancelReminder()`，输出成功/失败信息。
  - 末尾打印 `ESP_LOGI(TAG, "✅ Reminder tools initialized")`。

- ✅ 2025-11-01: 已完成，参见 `main/reminder_tools.cc`、`main/reminder_tools.h`。
### Step 1.2 ReminderManager 集成
- 在 `Application` 构造函数中为 `reminder_manager_` 注册回调：
  - 调用 `Schedule` 触发 `protocol_->SendWakeWordDetected`。
  - 可选：调用显示模块渲染提醒内容。
- 对外暴露 `ReminderManager& GetReminderManager()` 接口。

- ✅ 2025-11-01: 回调已在 `main/application.cc` 中保持生效，并新增 `GetReminderManager()` 接口。
### Step 1.3 Board 初始化
- 在 `main/boards/atk-dnesp32s3/atk_dnesp32s3.cc` 中引入 `reminder_tools.h`，并在构造流程里调用 `InitializeReminderTools()`。

- ✅ 2025-11-01: 已在 `main/boards/atk-dnesp32s3/atk_dnesp32s3.cc` 中增加 `InitializeTools()`，内部调用 `InitializeReminderTools()`。
### Step 1.4 CMake 与清理

### Step 1.5 提醒播报流程
- 目标：定时器触发后由小智播报提醒，不在设备端直接发声。
- 主要工作：
  1. 在 `MainEventLoop()` 处理提醒事件时，若设备正在 `speaking`/`listening`，先调用 `AbortSpeaking()` 或 `SendStopListening()` 结束会话并等待 idle。
  2. idle 后打开音频通道，发送固定唤醒词（如 `小智小智`），随后按照 `listen-start → listen-result → listen-stop` 顺序上报识别文本（例如“提醒时间到了吃药”）。
  3. 清空 `pending_reminder_message_`，保持重试机制（设备回到 idle 时若仍有缓存提醒需再次触发）。
- 预期影响：云端小智会像普通对话那样播报提醒内容，用户听到的是 AI 自然回复。

- 更新 `main/CMakeLists.txt` 把 `reminder_tools.cc` 添加到 `SRCS`。
- 删除 `application.cc` 中对 `[REMINDER_SET:...]` 文本命令的解析代码块。

- ✅ 2025-11-01: 已更新 `main/CMakeLists.txt` 并移除旧的 `[REMINDER_SET:...]` 解析逻辑。
## 阶段 2：构建与联调
1. **编译**：
   ```bash
   idf.py fullclean
   idf.py build
   ```
2. **烧录与监控**：
   ```bash
   idf.py -p COM3 flash monitor
   ```
3. **测试用例 A：倒计时提醒**
   - 用户：“小智，小智” → “10 秒后提醒我吃药”。
   - 期望：10 秒后设备播报“提醒：吃药”，云端返回自然语言确认。
   - 日志关键字：`Received reminder.add`、`Reminder created`、`Reminder #X triggered`。
  - 验证：串口应出现唤醒 → listen-result → listen-stop，扬声器播放来自小智的提醒。
4. **测试用例 B：定时提醒（跨天）**
   - 用户：“明天早上 7 点叫我起床”。
   - 期望：计算 delay ≈ 24 小时，次日触发提醒。
   - 日志关键字：`Calculated delay for time-based reminder`。
  - 验证：云端提醒回答必须来自小智；串口同样能看到 listen 流程。
5. **记录输出**：整理 monitor 日志，保存到 `docs/logs/reminder_<date>.log`（可选）。

## 阶段 3：可选增强
- **NVS 持久化**：
  - 在 `ReminderManager` 中新增 `LoadFromNVS()`、`SaveToNVS()`。
  - 触发条件：启动恢复、提醒变更时同步写入。
- **重复提醒**：
  - 扩展 `Reminder` 结构体，增加 `repeat_count`、`repeat_interval`。
  - `OnTimerExpired()` 中判断剩余次数并重新创建定时器。
- **提醒列表查询**：
  - `reminder.list` 工具返回 `count`、`reminders[]`（包含 `id`、`message`、`remaining_seconds`）。
- **UI 提示**：
  - 在显示层（如存在）显示当前提醒、剩余时间或历史记录。

## 测试验证标准
- 所有基础 TODO 勾选完成且提交至代码仓库。
- `idf.py build` 零错误，固件可烧录并稳定运行 ≥ 30 分钟。
- 倒计时/定时提醒案例均成功触发，日志完整。
- 未出现旧的 `[REMINDER_SET:...]` 解析日志或异常。

## 回滚策略
1. 在 Git 中保留变更分支，必要时 `git revert` 撤销合并提交。
2. 恢复 `mcp/reminder.py` 以继续沿用旧方案（临时应急）。
3. 使用备份的固件镜像烧录以快速恢复设备功能。

## 交付物
- 更新后的源代码与头文件。
- 联调日志与测试记录。
- 更新的实施文档与架构文档。
- （可选）固件二进制、演示视频或截图。

## 参考文档
- `完整架构文档.md`
- `docs/mcp-protocol.md`
- ESP-IDF v5.4 官方文档（定时器、NVS、FreeRTOS API）
